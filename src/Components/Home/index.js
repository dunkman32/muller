import React from 'react';
import MasterLayout from "../Layout";
import Header from "../Header";

const Index = () => {
    return (
        <MasterLayout>
            <Header/>
        </MasterLayout>
    );
};

export default Index;
